# minilisp-rs

A minimal Lisp interpreter written in Rust.

## Features

*   Lexer
*   Parser
*   Basic interpreter supporting arithmetic operations, comparisons, and lambda functions.

## Building and Running

```bash
# Build the project
cargo build

# Run the interpreter (it currently doesn't take input, modify main.rs to add/alter test lisp programs)
cargo run
```
